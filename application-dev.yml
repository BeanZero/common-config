server:
  port: 8001
spring:
  application:
    name: common-config-up2
  cloud:
    config:
      server:
        git:
          uri: https://github.com/beanzero/common-config/
#          search-paths:
#          username:
#          password: